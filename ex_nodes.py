# -*- coding: utf-8 -*-

# Modules
import hou

# Code
class NoiseAttributeTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _oceanFoam(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

    def _invokeCompiledBlock(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

    def _tableimport(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

    def _opencl(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

    def _mountain(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

    def _each(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

class EnumerateTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _ends(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

    def _nameFromGroups(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("trim")

    def _knife(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("object_merge")

class TransformTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _hole(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("hole")

    def _group(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("group")

    def _inflate(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("inflate")

    def _restPosition(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("rest")

class PolyWireTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _mirror(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("mirror")

    def _add(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("add")

    def _reverse(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("reverse")

class ExtractTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _dissolve(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("dissolve")

    def _agent(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("agent")

    def _lattice(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("lattice")

    def _boolean(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("boolean")

class NormalizeTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _output(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("output")

    def _isooffset(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("isooffset")

    def _line(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("line")

    def _lattice(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("lattice")

    def _instance(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("instance")

    def _matchAxis(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("match_axis")

class CacheTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _ikChains(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ik_chains")

    def _trim(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("trim")

    def _objectMerge(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("object_merge")

    def _xform(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("xform")

    def _explodedView(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("exploded_view")

class DeltaMushTemplate():
    def __init__():
        node = hou.node("/obj")
        self.geoNode = node.createNode("geo") 

    def _labsSplatter(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("labs::splatter")

    def _oceanWaves(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("ocean_waves")

    def _hairClump(self):
        if self.geoNode != None:
            node = self.geoNode.createNode("hair_clump")
